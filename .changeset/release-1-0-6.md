---
"mirror-orm": patch
---

Restore unscoped package name `mirror-orm`. The package was temporarily published as `@mirror-community/mirror-orm` during a migration attempt — it is now back under the original name. Users on `@mirror-community/mirror-orm` should switch back to `mirror-orm`.
